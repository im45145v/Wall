name: Keep Render Backend Awake

on:
  schedule:
    - cron: "*/14 * * * *"
  workflow_dispatch:

jobs:
  ping:
    runs-on: ubuntu-latest

    steps:
      - name: Ping backend health endpoint
        run: curl -s -o /dev/null -w "%{http_code}" https://wall-qwuk.onrender.com/api/health
